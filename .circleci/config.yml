version: 2.1
parameters:
  running-file:
    type: enum
    enum:
      [
        search_fixedasset/searching_full_flow.jmx,
        search_fixedasset/searchingTC4.jmx,
        searching/searchStagingLoginMultipleTime.jmx,
        searching/searchBetaBypassLogin.jmx,
      ]
    default: searching/searchStagingLoginMultipleTime.jmx

setup: true

orbs:
  aws-s3: circleci/aws-s3@3.0.0
  aws-cli: circleci/aws-cli@4.0.0
  aws-eks: circleci/aws-eks@2.2
  kubernetes: circleci/kubernetes@1.3
  jq: circleci/jq@2.2.0

workflows:
  common-test:
    jobs:
      - job_test:
          context:
            - deploy-role-load-test-cluster

jobs:
  job_test:
    docker:
      - image: cimg/base:current
    environment:
      AWS_DEFAULT_REGION: ap-northeast-1
      AWS_ACCOUNT_ID: "056699753121"
    steps:
      - checkout
      - aws-cli/setup:
          role_arn: ${AWS_ROLE_ARN}
          profile_name: "OIDC_PROFILE"
          session_duration: "1800"
      - kubernetes/install-kubectl:
          kubectl-version: v1.22.0
      - aws-eks/update-kubeconfig-with-authenticator:
          cluster-name: "load-test-cluster"
          aws-profile: "OIDC_PROFILE"
          aws-region: ${AWS_DEFAULT_REGION}
      - jq/install
      - run:
          name: install qapf-tools
          command: |
            echo "Download qapf"
            asseturl=$(curl -H "Authorization: token ${MINAMI_GITHUB_TOKEN}" https://api.github.com/repos/moneyforward/load-test-manifests/releases/latest | jq -r '.assets[] | select(.name=="qapf.zip") | .url')
            curl -L -H "Authorization: token ${MINAMI_GITHUB_TOKEN}" -H "Accept:application/octet-stream" ${asseturl} >qapf.zip
            unzip qapf.zip
            chmod +x ./qapf
      - run:
          name: Conduct performance test
          command: |
            ls -la
            echo "List all workspaces"
            ./qapf lists
            echo "Create workspace"
            ./qapf create-workspace ${CIRCLE_PROJECT_REPONAME}
            echo "Delete workspace"
            ./qapf delete-workspace ${CIRCLE_PROJECT_REPONAME}
      # - run:
      #     name: Make Results Directory
      #     command: mkdir results
      # - run:
      #     name: Remove output login data
      #     command: rm -rf services/search_fixedasset/ouput/account.csv
      # - run:
      #     name: Run test
      #     command: |
      #       jmeter -n -t services/<< pipeline.parameters.running-file >> -l results/runResults.csv -e -o results;
      # - store_artifacts:
      #     path: ./results
